server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/studiodb
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
